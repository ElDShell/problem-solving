#include <bits/stdc++.h>
#include <array>

using namespace std;

#define ll long long

int main (){
    ios_base::sync_with_stdio(false);cin.tie(0);cout.tie(0);

    array<int,5> arr;
    for(int i=0;i<5;i++)cin >> arr[i];
    for(int i=0;i<5;i++)cout << arr[i];
    vector<array<int,5>> d2; #2D
    return 0;
}